name: Simple CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    environment: 
      name: ENG
   
    steps:
    - name: Run a one-line script
      if: vars.ENV1 == 'toto' && vars.ENV2 == 'titi'
      run: |
        echo "test de variables OK : " ${{ format('Var {0} {1} {2}', vars.ENV1, '-', vars.ENV2) }}

    - name: display vars
      run: |
        echo "Var ENV1 : " ${{ vars.ENV1 }}

